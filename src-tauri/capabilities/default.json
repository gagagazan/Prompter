{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal core capabilities for Prompter webviews. Filesystem and system integrations stay in Rust commands.",
  "windows": ["manager", "launcher", "settings"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
